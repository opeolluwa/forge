# #!/bin/sh
# # Pre-commit hook: run lint before committing

echo "Running lint..."
cargo fmt
cargo sort
cargo group-imports --fix
if [ $? -ne 0 ]; then
  echo "❌ Lint failed. Commit aborted."
  exit 1
fi

echo "✅ Lint passed."